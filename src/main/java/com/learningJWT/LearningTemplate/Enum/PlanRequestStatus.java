package com.learningJWT.LearningTemplate.Enum;

/** Status of a library admin's request to change (upgrade/downgrade) their subscription plan. */
public enum PlanRequestStatus {
    PENDING,
    APPROVED,
    REJECTED
}
