spring.application.name=LearningTemplate

# Database Config
spring.datasource.url=jdbc:mysql://localhost:3306/LMS?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Raza1234
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Config
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.id.new_generator_mappings=false

# File upload (payment QR / payment proof screenshots)
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=10MB
app.upload.dir=uploads
app.base-url=http://localhost:8080
# Public URL of the frontend app — used to build the QR code content (e.g. http://<host>:5173)
# so any phone camera can scan it and land directly on /qr/{value} (attendance or self-registration).
# For LAN/mobile testing, set this to your machine's LAN IP, e.g. http://192.168.1.5:5173
app.frontend-url=http://localhost:5173




spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=ee7993578@gmail.com
spring.mail.password=juuumowlioctnohf
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Razorpay (payment gateway) — set these via environment variables in production,
# e.g. RAZORPAY_KEY_ID / RAZORPAY_KEY_SECRET / RAZORPAY_WEBHOOK_SECRET.
# NEVER commit real keys here. The test key visible in your screenshot has already been
# exposed publicly — rotate it in the Razorpay dashboard before using it anywhere real.
razorpay.key-id=rzp_test_T7NUegvZBN6YPn
razorpay.key-secret=KgonDHNr0Hxi2gVf6d62Yxis